{
  "buildCommand": "flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null,
  "installCommand": "echo 'Flutter SDK is pre-installed on Vercel'",
  "env": {
    "FLUTTER_VERSION": "3.41.0"
  }
}
